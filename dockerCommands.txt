******************************* DOCKER COMMANDS *************************

docker ps -- Show all containers and all
docker pull hello-world — to pull an image.
docker images — to see which images are available.
docker run hello-world — this will runs the image
docker build -t [image name]  [Path] — to make a docker image from docker file you can write . if you are at the path
docker run [image name] — to run that project

docker rmi -- used to delete images
docker volume ls -- user to list all volumes
docker images -- used to list all images
docker run [image name] — to run that project
docker run -p 3000:3000 [Image name]  — used to run on a particular port.

these commands are enough to learn docker ret will come from expirence.

Docker ko Production mai launch nhi kr skte kyuki uske crash hone ke chances zyada h so hm docker containers ko Orchestrate krte h Orchestration mtlb various docker containers ko manage krna.
For Example — If kisi container mai zyada traffic aagya toh scale krdeta h aur agr crash hogya toh use heal krdeta h.

